services:
  - type: web
    name: shadow-hunter-server
    runtime: docker
    dockerfilePath: deploy/Dockerfile
    plan: free
    healthCheckPath: /health
    buildFilter:
      paths:
        - deploy/**
        - scripts/**
        - scenes/**
        - data/**
